"use client";
import { useEffect, useState } from "react";
import { adminApi } from "@/lib/api";
type Health = Awaited<ReturnType<typeof adminApi.catalogHealth>>;
export default function HealthPage() { const [health, setHealth] = useState<Health | null>(null); useEffect(() => { void adminApi.catalogHealth().then(setHealth).catch(() => setHealth(null)); }, []); if (!health) return <p className="p-6 text-sm text-[var(--ds-color-muted-foreground)]">Carregando saúde do catálogo…</p>; const cards = [["Ofertas", health.totalOffers], ["Com marca", `${health.pctWithBrand}%`], ["Com canônico", `${health.pctWithCanonical}%`], ["Pendentes", health.pendingValidation], ["Suspeitas", health.suspicious], ["Produto em 1 mercado", health.singleMarketProducts]]; return <section className="space-y-5 p-6"><div><p className="ds-label-caps">Catálogo / Prisma</p><h1 className="text-2xl font-bold">Saúde do catálogo</h1><p className="text-sm text-[var(--ds-color-muted-foreground)]">Indicadores calculados diretamente das ofertas e produtos canônicos.</p></div><div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-3">{cards.map(([label, value]) => <div key={String(label)} className="rounded border border-[var(--ds-color-border)] p-4"><p className="text-xs text-[var(--ds-color-muted-foreground)]">{label}</p><p className="mt-1 text-2xl font-bold">{value}</p></div>)}</div></section>; }
