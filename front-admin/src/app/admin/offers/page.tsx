"use client";
import { useEffect, useState } from "react";
import { adminApi } from "@/lib/api";
type Offer = { id:string; name:string; price:string|number; validationStatus:string; supermarket:{name:string}; flyer:{title:string|null} };
export default function OffersPage() { const [items,setItems]=useState<Offer[]>(); const [error,setError]=useState(false); useEffect(()=>{void (async()=>{try{const page=await adminApi.offers();setItems(page.items)}catch{setError(true)}})()},[]); return <section className="mx-auto max-w-6xl space-y-6"><header><p className="text-sm font-medium text-emerald-700">Catálogo</p><h1 className="text-3xl font-semibold">Ofertas</h1></header>{error?<p className="text-red-700">Não foi possível carregar as ofertas.</p>:!items?<p>Carregando ofertas…</p>:<div className="overflow-hidden rounded-xl border bg-white"><table className="w-full text-sm"><thead><tr><th>Oferta</th><th>Rede</th><th>Preço</th><th>Status</th></tr></thead><tbody>{items.map(o=><tr key={o.id} className="border-t"><td>{o.name}</td><td>{o.supermarket.name}</td><td>R$ {String(o.price)}</td><td>{o.validationStatus}</td></tr>)}</tbody></table></div>}</section> }
